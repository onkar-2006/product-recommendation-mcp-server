[project]
name = "ecommerce-mcp"
version = "0.1.0"
description = "Production-grade E-Commerce Model Context Protocol Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
